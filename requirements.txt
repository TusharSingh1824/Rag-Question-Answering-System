# Core dependencies (required)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.21.0

# LLM Backend: Google Gemini (NEW SDK — replaces deprecated google-generativeai)
google-genai>=1.0.0

# ── Optional LLM Backends (install as needed) ──
# OpenAI:       pip install openai>=1.0.0
# Ollama:       pip install requests>=2.28.0    (+ install Ollama separately)
# HuggingFace:  pip install transformers>=4.30.0 torch>=2.0.0